import java.util.Scanner;
public class Program
{
	public static void main(String[] args) {
		Scanner scan=new Scanner(System.in);
		int first,last,j,counter=0;
		System.out.println("Enter the first nunber :");
		first=scan.nextInt();
		System.out.println("Enter the last nunber :");
		last=scan.nextInt();
		for (int i=first;i<=last;i++){
			counter=0;
		    for (j=1;j<=i;j++){
		        if (i%j==0)
		        	counter++;
		    }
		    if (counter==2)
		    	 System.out.println(i);
		}
		}
}
